#!/bin/bash
set -e

elixir --version

mix local.hex --force
mix local.rebar --force

# Make a local copy of livebook as a submodule
rm -fr livebook
git submodule add -f https://github.com/livebook-dev/livebook ./livebook
git submodule update --remote

(
  cd ./livebook

  mix deps.update --all
)
